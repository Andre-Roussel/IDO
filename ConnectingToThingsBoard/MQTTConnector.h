#include <MQTT.h>

const char mqttServer[] = ""; // IP adress of IDO ThingsBoard Server (Broker)
const int mqttServerPort = 1883; // broker mqtt port
const char key[] = ""; // broker device key
const char secret[] = ""; // broker secret not needed for our setup
const char device[] = ""; // broker device identifier
